/**
 * Simple greeting function.
 * @param name                  Name of person to greet.
 */
export const greeting = (name: string) => console.log('Hello, ' + name + '!');

// attempt greeting the package :)
greeting('TS-Bytenode');
